version: '3.8'

services:
  redis:
    image: redis:7.2.13-alpine
    container_name: redis
    hostname: redis
    ports:
      - "6379:6379"
    volumes:
      - D:/.redisdata:/data
    restart: unless-stopped
    networks:
      - redis-network

networks:
  redis-network:
    driver: bridge

